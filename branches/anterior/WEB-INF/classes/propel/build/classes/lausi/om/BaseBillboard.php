<?php

require_once 'propel/om/BaseObject.php';

require_once 'propel/om/Persistent.php';


include_once 'propel/util/Criteria.php';

include_once 'lausi/BillboardPeer.php';

/**
 * Base class that represents a row from the 'lausi_billboard' table.
 *
 * Base de Activos (Carteleras)
 *
 * This class was autogenerated by Propel on:
 *
 * 10/03/08 16:25:59
 *
 * @package    lausi.om
 */
abstract class BaseBillboard extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        BillboardPeer
	 */
	protected static $peer;


	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;


	/**
	 * The value for the number field.
	 * @var        int
	 */
	protected $number;


	/**
	 * The value for the type field.
	 * @var        int
	 */
	protected $type;


	/**
	 * The value for the height field.
	 * @var        boolean
	 */
	protected $height;


	/**
	 * The value for the row field.
	 * @var        int
	 */
	protected $row;


	/**
	 * The value for the billboardcolumn field.
	 * @var        int
	 */
	protected $billboardcolumn;


	/**
	 * The value for the addressid field.
	 * @var        int
	 */
	protected $addressid;

	/**
	 * @var        Address
	 */
	protected $aAddress;

	/**
	 * Collection to store aggregation of collAdvertisements.
	 * @var        array
	 */
	protected $collAdvertisements;

	/**
	 * The criteria used to select the current contents of collAdvertisements.
	 * @var        Criteria
	 */
	protected $lastAdvertisementCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	/**
	 * Get the [id] column value.
	 * Id del activo
	 * @return     int
	 */
	public function getId()
	{

		return $this->id;
	}

	/**
	 * Get the [number] column value.
	 * numero del activo
	 * @return     int
	 */
	public function getNumber()
	{

		return $this->number;
	}

	/**
	 * Get the [type] column value.
	 * tipo del activo
	 * @return     int
	 */
	public function getType()
	{

		return $this->type;
	}

	/**
	 * Get the [height] column value.
	 * si esta en altura
	 * @return     boolean
	 */
	public function getHeight()
	{

		return $this->height;
	}

	/**
	 * Get the [row] column value.
	 * fila del activo
	 * @return     int
	 */
	public function getRow()
	{

		return $this->row;
	}

	/**
	 * Get the [billboardcolumn] column value.
	 * columna del activo
	 * @return     int
	 */
	public function getBillboardcolumn()
	{

		return $this->billboardcolumn;
	}

	/**
	 * Get the [addressid] column value.
	 * altura del activo
	 * @return     int
	 */
	public function getAddressid()
	{

		return $this->addressid;
	}

	/**
	 * Set the value of [id] column.
	 * Id del activo
	 * @param      int $v new value
	 * @return     void
	 */
	public function setId($v)
	{

		// Since the native PHP type for this column is integer,
		// we will cast the input value to an int (if it is not).
		if ($v !== null && !is_int($v) && is_numeric($v)) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = BillboardPeer::ID;
		}

	} // setId()

	/**
	 * Set the value of [number] column.
	 * numero del activo
	 * @param      int $v new value
	 * @return     void
	 */
	public function setNumber($v)
	{

		// Since the native PHP type for this column is integer,
		// we will cast the input value to an int (if it is not).
		if ($v !== null && !is_int($v) && is_numeric($v)) {
			$v = (int) $v;
		}

		if ($this->number !== $v) {
			$this->number = $v;
			$this->modifiedColumns[] = BillboardPeer::NUMBER;
		}

	} // setNumber()

	/**
	 * Set the value of [type] column.
	 * tipo del activo
	 * @param      int $v new value
	 * @return     void
	 */
	public function setType($v)
	{

		// Since the native PHP type for this column is integer,
		// we will cast the input value to an int (if it is not).
		if ($v !== null && !is_int($v) && is_numeric($v)) {
			$v = (int) $v;
		}

		if ($this->type !== $v) {
			$this->type = $v;
			$this->modifiedColumns[] = BillboardPeer::TYPE;
		}

	} // setType()

	/**
	 * Set the value of [height] column.
	 * si esta en altura
	 * @param      boolean $v new value
	 * @return     void
	 */
	public function setHeight($v)
	{

		if ($this->height !== $v) {
			$this->height = $v;
			$this->modifiedColumns[] = BillboardPeer::HEIGHT;
		}

	} // setHeight()

	/**
	 * Set the value of [row] column.
	 * fila del activo
	 * @param      int $v new value
	 * @return     void
	 */
	public function setRow($v)
	{

		// Since the native PHP type for this column is integer,
		// we will cast the input value to an int (if it is not).
		if ($v !== null && !is_int($v) && is_numeric($v)) {
			$v = (int) $v;
		}

		if ($this->row !== $v) {
			$this->row = $v;
			$this->modifiedColumns[] = BillboardPeer::ROW;
		}

	} // setRow()

	/**
	 * Set the value of [billboardcolumn] column.
	 * columna del activo
	 * @param      int $v new value
	 * @return     void
	 */
	public function setBillboardcolumn($v)
	{

		// Since the native PHP type for this column is integer,
		// we will cast the input value to an int (if it is not).
		if ($v !== null && !is_int($v) && is_numeric($v)) {
			$v = (int) $v;
		}

		if ($this->billboardcolumn !== $v) {
			$this->billboardcolumn = $v;
			$this->modifiedColumns[] = BillboardPeer::BILLBOARDCOLUMN;
		}

	} // setBillboardcolumn()

	/**
	 * Set the value of [addressid] column.
	 * altura del activo
	 * @param      int $v new value
	 * @return     void
	 */
	public function setAddressid($v)
	{

		// Since the native PHP type for this column is integer,
		// we will cast the input value to an int (if it is not).
		if ($v !== null && !is_int($v) && is_numeric($v)) {
			$v = (int) $v;
		}

		if ($this->addressid !== $v) {
			$this->addressid = $v;
			$this->modifiedColumns[] = BillboardPeer::ADDRESSID;
		}

		if ($this->aAddress !== null && $this->aAddress->getId() !== $v) {
			$this->aAddress = null;
		}

	} // setAddressid()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (1-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      ResultSet $rs The ResultSet class with cursor advanced to desired record pos.
	 * @param      int $startcol 1-based offset column which indicates which restultset column to start with.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate(ResultSet $rs, $startcol = 1)
	{
		try {

			$this->id = $rs->getInt($startcol + 0);

			$this->number = $rs->getInt($startcol + 1);

			$this->type = $rs->getInt($startcol + 2);

			$this->height = $rs->getBoolean($startcol + 3);

			$this->row = $rs->getInt($startcol + 4);

			$this->billboardcolumn = $rs->getInt($startcol + 5);

			$this->addressid = $rs->getInt($startcol + 6);

			$this->resetModified();

			$this->setNew(false);

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 7; // 7 = BillboardPeer::NUM_COLUMNS - BillboardPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Billboard object", $e);
		}
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      Connection $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete($con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(BillboardPeer::DATABASE_NAME);
		}

		try {
			$con->begin();
			BillboardPeer::doDelete($this, $con);
			$this->setDeleted(true);
			$con->commit();
		} catch (PropelException $e) {
			$con->rollback();
			throw $e;
		}
	}

	/**
	 * Stores the object in the database.  If the object is new,
	 * it inserts it; otherwise an update is performed.  This method
	 * wraps the doSave() worker method in a transaction.
	 *
	 * @param      Connection $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save($con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(BillboardPeer::DATABASE_NAME);
		}

		try {
			$con->begin();
			$affectedRows = $this->doSave($con);
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollback();
			throw $e;
		}
	}

	/**
	 * Stores the object in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      Connection $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave($con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;


			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aAddress !== null) {
				if ($this->aAddress->isModified()) {
					$affectedRows += $this->aAddress->save($con);
				}
				$this->setAddress($this->aAddress);
			}


			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = BillboardPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += BillboardPeer::doUpdate($this, $con);
				}
				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collAdvertisements !== null) {
				foreach($this->collAdvertisements as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;
		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aAddress !== null) {
				if (!$this->aAddress->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aAddress->getValidationFailures());
				}
			}


			if (($retval = BillboardPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collAdvertisements !== null) {
					foreach($this->collAdvertisements as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(BillboardPeer::DATABASE_NAME);

		if ($this->isColumnModified(BillboardPeer::ID)) $criteria->add(BillboardPeer::ID, $this->id);
		if ($this->isColumnModified(BillboardPeer::NUMBER)) $criteria->add(BillboardPeer::NUMBER, $this->number);
		if ($this->isColumnModified(BillboardPeer::TYPE)) $criteria->add(BillboardPeer::TYPE, $this->type);
		if ($this->isColumnModified(BillboardPeer::HEIGHT)) $criteria->add(BillboardPeer::HEIGHT, $this->height);
		if ($this->isColumnModified(BillboardPeer::ROW)) $criteria->add(BillboardPeer::ROW, $this->row);
		if ($this->isColumnModified(BillboardPeer::BILLBOARDCOLUMN)) $criteria->add(BillboardPeer::BILLBOARDCOLUMN, $this->billboardcolumn);
		if ($this->isColumnModified(BillboardPeer::ADDRESSID)) $criteria->add(BillboardPeer::ADDRESSID, $this->addressid);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(BillboardPeer::DATABASE_NAME);

		$criteria->add(BillboardPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Billboard (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setNumber($this->number);

		$copyObj->setType($this->type);

		$copyObj->setHeight($this->height);

		$copyObj->setRow($this->row);

		$copyObj->setBillboardcolumn($this->billboardcolumn);

		$copyObj->setAddressid($this->addressid);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach($this->getAdvertisements() as $relObj) {
				$copyObj->addAdvertisement($relObj->copy($deepCopy));
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a pkey column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Billboard Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     BillboardPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new BillboardPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a Address object.
	 *
	 * @param      Address $v
	 * @return     void
	 * @throws     PropelException
	 */
	public function setAddress($v)
	{


		if ($v === null) {
			$this->setAddressid(NULL);
		} else {
			$this->setAddressid($v->getId());
		}


		$this->aAddress = $v;
	}


	/**
	 * Get the associated Address object
	 *
	 * @param      Connection Optional Connection object.
	 * @return     Address The associated Address object.
	 * @throws     PropelException
	 */
	public function getAddress($con = null)
	{
		// include the related Peer class
		include_once 'lausi/om/BaseAddressPeer.php';

		if ($this->aAddress === null && ($this->addressid !== null)) {

			$this->aAddress = AddressPeer::retrieveByPK($this->addressid, $con);

			/* The following can be used instead of the line above to
			   guarantee the related object contains a reference
			   to this object, but this level of coupling
			   may be undesirable in many circumstances.
			   As it can lead to a db query with many results that may
			   never be used.
			   $obj = AddressPeer::retrieveByPK($this->addressid, $con);
			   $obj->addAddresss($this);
			 */
		}
		return $this->aAddress;
	}

	/**
	 * Temporary storage of collAdvertisements to save a possible db hit in
	 * the event objects are add to the collection, but the
	 * complete collection is never requested.
	 * @return     void
	 */
	public function initAdvertisements()
	{
		if ($this->collAdvertisements === null) {
			$this->collAdvertisements = array();
		}
	}

	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Billboard has previously
	 * been saved, it will retrieve related Advertisements from storage.
	 * If this Billboard is new, it will return
	 * an empty collection or the current collection, the criteria
	 * is ignored on a new object.
	 *
	 * @param      Connection $con
	 * @param      Criteria $criteria
	 * @throws     PropelException
	 */
	public function getAdvertisements($criteria = null, $con = null)
	{
		// include the Peer class
		include_once 'lausi/om/BaseAdvertisementPeer.php';
		if ($criteria === null) {
			$criteria = new Criteria();
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAdvertisements === null) {
			if ($this->isNew()) {
			   $this->collAdvertisements = array();
			} else {

				$criteria->add(AdvertisementPeer::BILLBOARDID, $this->getId());

				AdvertisementPeer::addSelectColumns($criteria);
				$this->collAdvertisements = AdvertisementPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(AdvertisementPeer::BILLBOARDID, $this->getId());

				AdvertisementPeer::addSelectColumns($criteria);
				if (!isset($this->lastAdvertisementCriteria) || !$this->lastAdvertisementCriteria->equals($criteria)) {
					$this->collAdvertisements = AdvertisementPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastAdvertisementCriteria = $criteria;
		return $this->collAdvertisements;
	}

	/**
	 * Returns the number of related Advertisements.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      Connection $con
	 * @throws     PropelException
	 */
	public function countAdvertisements($criteria = null, $distinct = false, $con = null)
	{
		// include the Peer class
		include_once 'lausi/om/BaseAdvertisementPeer.php';
		if ($criteria === null) {
			$criteria = new Criteria();
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		$criteria->add(AdvertisementPeer::BILLBOARDID, $this->getId());

		return AdvertisementPeer::doCount($criteria, $distinct, $con);
	}

	/**
	 * Method called to associate a Advertisement object to this object
	 * through the Advertisement foreign key attribute
	 *
	 * @param      Advertisement $l Advertisement
	 * @return     void
	 * @throws     PropelException
	 */
	public function addAdvertisement(Advertisement $l)
	{
		$this->collAdvertisements[] = $l;
		$l->setBillboard($this);
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Billboard is new, it will return
	 * an empty collection; or if this Billboard has previously
	 * been saved, it will retrieve related Advertisements from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Billboard.
	 */
	public function getAdvertisementsJoinTheme($criteria = null, $con = null)
	{
		// include the Peer class
		include_once 'lausi/om/BaseAdvertisementPeer.php';
		if ($criteria === null) {
			$criteria = new Criteria();
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAdvertisements === null) {
			if ($this->isNew()) {
				$this->collAdvertisements = array();
			} else {

				$criteria->add(AdvertisementPeer::BILLBOARDID, $this->getId());

				$this->collAdvertisements = AdvertisementPeer::doSelectJoinTheme($criteria, $con);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(AdvertisementPeer::BILLBOARDID, $this->getId());

			if (!isset($this->lastAdvertisementCriteria) || !$this->lastAdvertisementCriteria->equals($criteria)) {
				$this->collAdvertisements = AdvertisementPeer::doSelectJoinTheme($criteria, $con);
			}
		}
		$this->lastAdvertisementCriteria = $criteria;

		return $this->collAdvertisements;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Billboard is new, it will return
	 * an empty collection; or if this Billboard has previously
	 * been saved, it will retrieve related Advertisements from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Billboard.
	 */
	public function getAdvertisementsJoinWorkforce($criteria = null, $con = null)
	{
		// include the Peer class
		include_once 'lausi/om/BaseAdvertisementPeer.php';
		if ($criteria === null) {
			$criteria = new Criteria();
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAdvertisements === null) {
			if ($this->isNew()) {
				$this->collAdvertisements = array();
			} else {

				$criteria->add(AdvertisementPeer::BILLBOARDID, $this->getId());

				$this->collAdvertisements = AdvertisementPeer::doSelectJoinWorkforce($criteria, $con);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(AdvertisementPeer::BILLBOARDID, $this->getId());

			if (!isset($this->lastAdvertisementCriteria) || !$this->lastAdvertisementCriteria->equals($criteria)) {
				$this->collAdvertisements = AdvertisementPeer::doSelectJoinWorkforce($criteria, $con);
			}
		}
		$this->lastAdvertisementCriteria = $criteria;

		return $this->collAdvertisements;
	}

} // BaseBillboard
