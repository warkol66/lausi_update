<?php

require_once 'propel/om/BaseObject.php';

require_once 'propel/om/Persistent.php';


include_once 'propel/util/Criteria.php';

include_once 'lausi/ThemePeer.php';

/**
 * Base class that represents a row from the 'lausi_theme' table.
 *
 * Base de Motivos
 *
 * This class was autogenerated by Propel on:
 *
 * 10/03/08 16:25:59
 *
 * @package    lausi.om
 */
abstract class BaseTheme extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        ThemePeer
	 */
	protected static $peer;


	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;


	/**
	 * The value for the name field.
	 * @var        string
	 */
	protected $name;


	/**
	 * The value for the shortname field.
	 * @var        string
	 */
	protected $shortname;


	/**
	 * The value for the startdate field.
	 * @var        int
	 */
	protected $startdate;


	/**
	 * The value for the duration field.
	 * @var        int
	 */
	protected $duration;


	/**
	 * The value for the sheetstotal field.
	 * @var        int
	 */
	protected $sheetstotal;


	/**
	 * The value for the type field.
	 * @var        int
	 */
	protected $type;


	/**
	 * The value for the active field.
	 * @var        boolean
	 */
	protected $active = true;


	/**
	 * The value for the clientid field.
	 * @var        int
	 */
	protected $clientid;

	/**
	 * @var        Client
	 */
	protected $aClient;

	/**
	 * Collection to store aggregation of collAdvertisements.
	 * @var        array
	 */
	protected $collAdvertisements;

	/**
	 * The criteria used to select the current contents of collAdvertisements.
	 * @var        Criteria
	 */
	protected $lastAdvertisementCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	/**
	 * Get the [id] column value.
	 * Id del motivo
	 * @return     int
	 */
	public function getId()
	{

		return $this->id;
	}

	/**
	 * Get the [name] column value.
	 * nombre del motivo
	 * @return     string
	 */
	public function getName()
	{

		return $this->name;
	}

	/**
	 * Get the [shortname] column value.
	 * nombre abreviado del motivo
	 * @return     string
	 */
	public function getShortname()
	{

		return $this->shortname;
	}

	/**
	 * Get the [optionally formatted] [startdate] column value.
	 * fecha de inicio del motivo
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the integer unix timestamp will be returned.
	 * @return     mixed Formatted date/time value as string or integer unix timestamp (if format is NULL).
	 * @throws     PropelException - if unable to convert the date/time to timestamp.
	 */
	public function getStartdate($format = '%Y/%m/%d')
	{

		if ($this->startdate === null || $this->startdate === '') {
			return null;
		} elseif (!is_int($this->startdate)) {
			// a non-timestamp value was set externally, so we convert it
			$ts = strtotime($this->startdate);
			if ($ts === -1 || $ts === false) { // in PHP 5.1 return value changes to FALSE
				throw new PropelException("Unable to parse value of [startdate] as date/time value: " . var_export($this->startdate, true));
			}
		} else {
			$ts = $this->startdate;
		}
		if ($format === null) {
			return $ts;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $ts);
		} else {
			return date($format, $ts);
		}
	}

	/**
	 * Get the [duration] column value.
	 * duracion del motivo
	 * @return     int
	 */
	public function getDuration()
	{

		return $this->duration;
	}

	/**
	 * Get the [sheetstotal] column value.
	 * cantidad total de afiches que deberan distribuirse del motivo
	 * @return     int
	 */
	public function getSheetstotal()
	{

		return $this->sheetstotal;
	}

	/**
	 * Get the [type] column value.
	 * tipo del motivo
	 * @return     int
	 */
	public function getType()
	{

		return $this->type;
	}

	/**
	 * Get the [active] column value.
	 * indica si el motivo esta activo o no
	 * @return     boolean
	 */
	public function getActive()
	{

		return $this->active;
	}

	/**
	 * Get the [clientid] column value.
	 * Id del cliente del motivo
	 * @return     int
	 */
	public function getClientid()
	{

		return $this->clientid;
	}

	/**
	 * Set the value of [id] column.
	 * Id del motivo
	 * @param      int $v new value
	 * @return     void
	 */
	public function setId($v)
	{

		// Since the native PHP type for this column is integer,
		// we will cast the input value to an int (if it is not).
		if ($v !== null && !is_int($v) && is_numeric($v)) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = ThemePeer::ID;
		}

	} // setId()

	/**
	 * Set the value of [name] column.
	 * nombre del motivo
	 * @param      string $v new value
	 * @return     void
	 */
	public function setName($v)
	{

		// Since the native PHP type for this column is string,
		// we will cast the input to a string (if it is not).
		if ($v !== null && !is_string($v)) {
			$v = (string) $v; 
		}

		if ($this->name !== $v) {
			$this->name = $v;
			$this->modifiedColumns[] = ThemePeer::NAME;
		}

	} // setName()

	/**
	 * Set the value of [shortname] column.
	 * nombre abreviado del motivo
	 * @param      string $v new value
	 * @return     void
	 */
	public function setShortname($v)
	{

		// Since the native PHP type for this column is string,
		// we will cast the input to a string (if it is not).
		if ($v !== null && !is_string($v)) {
			$v = (string) $v; 
		}

		if ($this->shortname !== $v) {
			$this->shortname = $v;
			$this->modifiedColumns[] = ThemePeer::SHORTNAME;
		}

	} // setShortname()

	/**
	 * Set the value of [startdate] column.
	 * fecha de inicio del motivo
	 * @param      int $v new value
	 * @return     void
	 */
	public function setStartdate($v)
	{

		if ($v !== null && !is_int($v)) {
			$ts = strtotime($v);
			if ($ts === -1 || $ts === false) { // in PHP 5.1 return value changes to FALSE
				throw new PropelException("Unable to parse date/time value for [startdate] from input: " . var_export($v, true));
			}
		} else {
			$ts = $v;
		}
		if ($this->startdate !== $ts) {
			$this->startdate = $ts;
			$this->modifiedColumns[] = ThemePeer::STARTDATE;
		}

	} // setStartdate()

	/**
	 * Set the value of [duration] column.
	 * duracion del motivo
	 * @param      int $v new value
	 * @return     void
	 */
	public function setDuration($v)
	{

		// Since the native PHP type for this column is integer,
		// we will cast the input value to an int (if it is not).
		if ($v !== null && !is_int($v) && is_numeric($v)) {
			$v = (int) $v;
		}

		if ($this->duration !== $v) {
			$this->duration = $v;
			$this->modifiedColumns[] = ThemePeer::DURATION;
		}

	} // setDuration()

	/**
	 * Set the value of [sheetstotal] column.
	 * cantidad total de afiches que deberan distribuirse del motivo
	 * @param      int $v new value
	 * @return     void
	 */
	public function setSheetstotal($v)
	{

		// Since the native PHP type for this column is integer,
		// we will cast the input value to an int (if it is not).
		if ($v !== null && !is_int($v) && is_numeric($v)) {
			$v = (int) $v;
		}

		if ($this->sheetstotal !== $v) {
			$this->sheetstotal = $v;
			$this->modifiedColumns[] = ThemePeer::SHEETSTOTAL;
		}

	} // setSheetstotal()

	/**
	 * Set the value of [type] column.
	 * tipo del motivo
	 * @param      int $v new value
	 * @return     void
	 */
	public function setType($v)
	{

		// Since the native PHP type for this column is integer,
		// we will cast the input value to an int (if it is not).
		if ($v !== null && !is_int($v) && is_numeric($v)) {
			$v = (int) $v;
		}

		if ($this->type !== $v) {
			$this->type = $v;
			$this->modifiedColumns[] = ThemePeer::TYPE;
		}

	} // setType()

	/**
	 * Set the value of [active] column.
	 * indica si el motivo esta activo o no
	 * @param      boolean $v new value
	 * @return     void
	 */
	public function setActive($v)
	{

		if ($this->active !== $v || $v === true) {
			$this->active = $v;
			$this->modifiedColumns[] = ThemePeer::ACTIVE;
		}

	} // setActive()

	/**
	 * Set the value of [clientid] column.
	 * Id del cliente del motivo
	 * @param      int $v new value
	 * @return     void
	 */
	public function setClientid($v)
	{

		// Since the native PHP type for this column is integer,
		// we will cast the input value to an int (if it is not).
		if ($v !== null && !is_int($v) && is_numeric($v)) {
			$v = (int) $v;
		}

		if ($this->clientid !== $v) {
			$this->clientid = $v;
			$this->modifiedColumns[] = ThemePeer::CLIENTID;
		}

		if ($this->aClient !== null && $this->aClient->getId() !== $v) {
			$this->aClient = null;
		}

	} // setClientid()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (1-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      ResultSet $rs The ResultSet class with cursor advanced to desired record pos.
	 * @param      int $startcol 1-based offset column which indicates which restultset column to start with.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate(ResultSet $rs, $startcol = 1)
	{
		try {

			$this->id = $rs->getInt($startcol + 0);

			$this->name = $rs->getString($startcol + 1);

			$this->shortname = $rs->getString($startcol + 2);

			$this->startdate = $rs->getDate($startcol + 3, null);

			$this->duration = $rs->getInt($startcol + 4);

			$this->sheetstotal = $rs->getInt($startcol + 5);

			$this->type = $rs->getInt($startcol + 6);

			$this->active = $rs->getBoolean($startcol + 7);

			$this->clientid = $rs->getInt($startcol + 8);

			$this->resetModified();

			$this->setNew(false);

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 9; // 9 = ThemePeer::NUM_COLUMNS - ThemePeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Theme object", $e);
		}
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      Connection $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete($con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(ThemePeer::DATABASE_NAME);
		}

		try {
			$con->begin();
			ThemePeer::doDelete($this, $con);
			$this->setDeleted(true);
			$con->commit();
		} catch (PropelException $e) {
			$con->rollback();
			throw $e;
		}
	}

	/**
	 * Stores the object in the database.  If the object is new,
	 * it inserts it; otherwise an update is performed.  This method
	 * wraps the doSave() worker method in a transaction.
	 *
	 * @param      Connection $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save($con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(ThemePeer::DATABASE_NAME);
		}

		try {
			$con->begin();
			$affectedRows = $this->doSave($con);
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollback();
			throw $e;
		}
	}

	/**
	 * Stores the object in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      Connection $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave($con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;


			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aClient !== null) {
				if ($this->aClient->isModified()) {
					$affectedRows += $this->aClient->save($con);
				}
				$this->setClient($this->aClient);
			}


			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = ThemePeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += ThemePeer::doUpdate($this, $con);
				}
				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collAdvertisements !== null) {
				foreach($this->collAdvertisements as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;
		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aClient !== null) {
				if (!$this->aClient->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aClient->getValidationFailures());
				}
			}


			if (($retval = ThemePeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collAdvertisements !== null) {
					foreach($this->collAdvertisements as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(ThemePeer::DATABASE_NAME);

		if ($this->isColumnModified(ThemePeer::ID)) $criteria->add(ThemePeer::ID, $this->id);
		if ($this->isColumnModified(ThemePeer::NAME)) $criteria->add(ThemePeer::NAME, $this->name);
		if ($this->isColumnModified(ThemePeer::SHORTNAME)) $criteria->add(ThemePeer::SHORTNAME, $this->shortname);
		if ($this->isColumnModified(ThemePeer::STARTDATE)) $criteria->add(ThemePeer::STARTDATE, $this->startdate);
		if ($this->isColumnModified(ThemePeer::DURATION)) $criteria->add(ThemePeer::DURATION, $this->duration);
		if ($this->isColumnModified(ThemePeer::SHEETSTOTAL)) $criteria->add(ThemePeer::SHEETSTOTAL, $this->sheetstotal);
		if ($this->isColumnModified(ThemePeer::TYPE)) $criteria->add(ThemePeer::TYPE, $this->type);
		if ($this->isColumnModified(ThemePeer::ACTIVE)) $criteria->add(ThemePeer::ACTIVE, $this->active);
		if ($this->isColumnModified(ThemePeer::CLIENTID)) $criteria->add(ThemePeer::CLIENTID, $this->clientid);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(ThemePeer::DATABASE_NAME);

		$criteria->add(ThemePeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Theme (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setName($this->name);

		$copyObj->setShortname($this->shortname);

		$copyObj->setStartdate($this->startdate);

		$copyObj->setDuration($this->duration);

		$copyObj->setSheetstotal($this->sheetstotal);

		$copyObj->setType($this->type);

		$copyObj->setActive($this->active);

		$copyObj->setClientid($this->clientid);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach($this->getAdvertisements() as $relObj) {
				$copyObj->addAdvertisement($relObj->copy($deepCopy));
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a pkey column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Theme Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     ThemePeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new ThemePeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a Client object.
	 *
	 * @param      Client $v
	 * @return     void
	 * @throws     PropelException
	 */
	public function setClient($v)
	{


		if ($v === null) {
			$this->setClientid(NULL);
		} else {
			$this->setClientid($v->getId());
		}


		$this->aClient = $v;
	}


	/**
	 * Get the associated Client object
	 *
	 * @param      Connection Optional Connection object.
	 * @return     Client The associated Client object.
	 * @throws     PropelException
	 */
	public function getClient($con = null)
	{
		// include the related Peer class
		include_once 'lausi/om/BaseClientPeer.php';

		if ($this->aClient === null && ($this->clientid !== null)) {

			$this->aClient = ClientPeer::retrieveByPK($this->clientid, $con);

			/* The following can be used instead of the line above to
			   guarantee the related object contains a reference
			   to this object, but this level of coupling
			   may be undesirable in many circumstances.
			   As it can lead to a db query with many results that may
			   never be used.
			   $obj = ClientPeer::retrieveByPK($this->clientid, $con);
			   $obj->addClients($this);
			 */
		}
		return $this->aClient;
	}

	/**
	 * Temporary storage of collAdvertisements to save a possible db hit in
	 * the event objects are add to the collection, but the
	 * complete collection is never requested.
	 * @return     void
	 */
	public function initAdvertisements()
	{
		if ($this->collAdvertisements === null) {
			$this->collAdvertisements = array();
		}
	}

	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Theme has previously
	 * been saved, it will retrieve related Advertisements from storage.
	 * If this Theme is new, it will return
	 * an empty collection or the current collection, the criteria
	 * is ignored on a new object.
	 *
	 * @param      Connection $con
	 * @param      Criteria $criteria
	 * @throws     PropelException
	 */
	public function getAdvertisements($criteria = null, $con = null)
	{
		// include the Peer class
		include_once 'lausi/om/BaseAdvertisementPeer.php';
		if ($criteria === null) {
			$criteria = new Criteria();
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAdvertisements === null) {
			if ($this->isNew()) {
			   $this->collAdvertisements = array();
			} else {

				$criteria->add(AdvertisementPeer::THEMEID, $this->getId());

				AdvertisementPeer::addSelectColumns($criteria);
				$this->collAdvertisements = AdvertisementPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(AdvertisementPeer::THEMEID, $this->getId());

				AdvertisementPeer::addSelectColumns($criteria);
				if (!isset($this->lastAdvertisementCriteria) || !$this->lastAdvertisementCriteria->equals($criteria)) {
					$this->collAdvertisements = AdvertisementPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastAdvertisementCriteria = $criteria;
		return $this->collAdvertisements;
	}

	/**
	 * Returns the number of related Advertisements.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      Connection $con
	 * @throws     PropelException
	 */
	public function countAdvertisements($criteria = null, $distinct = false, $con = null)
	{
		// include the Peer class
		include_once 'lausi/om/BaseAdvertisementPeer.php';
		if ($criteria === null) {
			$criteria = new Criteria();
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		$criteria->add(AdvertisementPeer::THEMEID, $this->getId());

		return AdvertisementPeer::doCount($criteria, $distinct, $con);
	}

	/**
	 * Method called to associate a Advertisement object to this object
	 * through the Advertisement foreign key attribute
	 *
	 * @param      Advertisement $l Advertisement
	 * @return     void
	 * @throws     PropelException
	 */
	public function addAdvertisement(Advertisement $l)
	{
		$this->collAdvertisements[] = $l;
		$l->setTheme($this);
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Theme is new, it will return
	 * an empty collection; or if this Theme has previously
	 * been saved, it will retrieve related Advertisements from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Theme.
	 */
	public function getAdvertisementsJoinBillboard($criteria = null, $con = null)
	{
		// include the Peer class
		include_once 'lausi/om/BaseAdvertisementPeer.php';
		if ($criteria === null) {
			$criteria = new Criteria();
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAdvertisements === null) {
			if ($this->isNew()) {
				$this->collAdvertisements = array();
			} else {

				$criteria->add(AdvertisementPeer::THEMEID, $this->getId());

				$this->collAdvertisements = AdvertisementPeer::doSelectJoinBillboard($criteria, $con);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(AdvertisementPeer::THEMEID, $this->getId());

			if (!isset($this->lastAdvertisementCriteria) || !$this->lastAdvertisementCriteria->equals($criteria)) {
				$this->collAdvertisements = AdvertisementPeer::doSelectJoinBillboard($criteria, $con);
			}
		}
		$this->lastAdvertisementCriteria = $criteria;

		return $this->collAdvertisements;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Theme is new, it will return
	 * an empty collection; or if this Theme has previously
	 * been saved, it will retrieve related Advertisements from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Theme.
	 */
	public function getAdvertisementsJoinWorkforce($criteria = null, $con = null)
	{
		// include the Peer class
		include_once 'lausi/om/BaseAdvertisementPeer.php';
		if ($criteria === null) {
			$criteria = new Criteria();
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAdvertisements === null) {
			if ($this->isNew()) {
				$this->collAdvertisements = array();
			} else {

				$criteria->add(AdvertisementPeer::THEMEID, $this->getId());

				$this->collAdvertisements = AdvertisementPeer::doSelectJoinWorkforce($criteria, $con);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(AdvertisementPeer::THEMEID, $this->getId());

			if (!isset($this->lastAdvertisementCriteria) || !$this->lastAdvertisementCriteria->equals($criteria)) {
				$this->collAdvertisements = AdvertisementPeer::doSelectJoinWorkforce($criteria, $con);
			}
		}
		$this->lastAdvertisementCriteria = $criteria;

		return $this->collAdvertisements;
	}

} // BaseTheme
