<?php 

require_once 'propel/om/BaseObject.php';

 
require_once 'propel/om/Persistent.php';

 

include_once 'propel/util/Criteria.php';
 

// (on-demand) include_once 'mluse/MLUseLanguage.php';
// (on-demand) include_once 'mluse/MLUseLanguagePeer.php';

include_once 'mluse/MLUseTextPeer.php';

/**
 * Base class that represents a row from the 'MLUSE_Text' table.
 *
 *  
 *
 * This class was autogenerated by Propel on:
 *
 * [09/27/06 18:29:52]
 *
 * You should not use this class directly.  It should not even be
 * extended; all references should be to MLUseText class. 
 * 
 * @package mluse 
 */
abstract class BaseMLUseText extends BaseObject implements Persistent {
	
	/** 
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var MLUseTextPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var int	  
	 */
	protected $id;	

	/**
	 * The value for the languageid field.
	 * @var int	  
	 */
	protected $languageid;	

	/**
	 * The value for the text field.
	 * @var string	  
	 */
	protected $text;	
  
	/**
	 * Get the Id column value.
	 *  
	 * @return int	  
	 */
	public function getId()
	{
		return $this->id;
	}


	/**
	 * Set the value of `id` column.	  
	 *  
	 * @param int $v new value
	 * @return void
	 *  
	 */
	public function setId($v)
	{
		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = MLUseTextPeer::ID;
		}
				
	}

  
	/**
	 * Get the Languageid column value.
	 *  
	 * @return int	  
	 */
	public function getLanguageid()
	{
		return $this->languageid;
	}


	/**
	 * Set the value of `languageid` column.	  
	 *  
	 * @param int $v new value
	 * @return void
	 * @throws PropelException 
	 */
	public function setLanguageid($v)
	{
		if ($this->languageid !== $v) {
			$this->languageid = $v;
			$this->modifiedColumns[] = MLUseTextPeer::LANGUAGEID;
		}
			
		if ($this->aMLUseLanguage !== null && $this->aMLUseLanguage->getId() !== $v) {
			$this->aMLUseLanguage = null;
		}
			
	}

  
	/**
	 * Get the Text column value.
	 *  
	 * @return string	  
	 */
	public function getText()
	{
		return $this->text;
	}


	/**
	 * Set the value of `text` column.	  
	 *  
	 * @param string $v new value
	 * @return void
	 *  
	 */
	public function setText($v)
	{
		if ($this->text !== $v) {
			$this->text = $v;
			$this->modifiedColumns[] = MLUseTextPeer::TEXT;
		}
				
	}


	
	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (1-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param ResultSet $rs The ResultSet class with cursor advanced to desired record pos.
	 * @param int $startcol 1-based offset column which indicates which restultset column to start with.
	 * @return void
	 * @throws PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate(ResultSet $rs, $startcol = 1)
	{
		try {
			$this->id = $rs->getInt($startcol + 0);					
			$this->languageid = $rs->getInt($startcol + 1);					
			$this->text = $rs->getString($startcol + 2);					
			 
			$this->resetModified();
			$this->setNew(false);
		
		} catch (Exception $e) {
			throw new PropelException("Error populating MLUseText object", $e);
		}		
	
	}
	
	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 * 
	 * @return Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{		
		$criteria = new Criteria(MLUseTextPeer::DATABASE_NAME);
		$criteria->add(MLUseTextPeer::ID, $this->id);
		$criteria->add(MLUseTextPeer::LANGUAGEID, $this->languageid);
		return $criteria;			
	}
	
	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{		
		$criteria = new Criteria(MLUseTextPeer::DATABASE_NAME);
		if ($this->isColumnModified(MLUseTextPeer::ID)) $criteria->add(MLUseTextPeer::ID, $this->id);
		if ($this->isColumnModified(MLUseTextPeer::LANGUAGEID)) $criteria->add(MLUseTextPeer::LANGUAGEID, $this->languageid);
		if ($this->isColumnModified(MLUseTextPeer::TEXT)) $criteria->add(MLUseTextPeer::TEXT, $this->text);
		return $criteria;			
	}
	
	

	/**
	 * @var MLUseLanguage	  
	 */
	protected $aMLUseLanguage;

	/**
	 * Declares an association between this object and a MLUseLanguage object
	 *
	 * @param MLUseLanguage $v
	 * @return void
	 * @throws PropelException
	 */
	public function setMLUseLanguage($v)
	{
			
		if ($v === null) {
			$this->setLanguageid(NULL);
		} else {
			$this->setLanguageid($v->getId());
		}
   
		$this->aMLUseLanguage = $v;
	}


	/**
	 * Get the associated MLUseLanguage object
	 *
	 * @param Connection Optional Connection object.
	 * @return MLUseLanguage The associated MLUseLanguage object.
	 * @throws PropelException
	 */
	public function getMLUseLanguage($con = null)
	{
		// include the Peer class
		include_once 'mluse/MLUseLanguagePeer.php';

		if ($this->aMLUseLanguage === null && ($this->languageid !== null)) {
	
			$this->aMLUseLanguage = MLUseLanguagePeer::retrieveByPK($this->languageid, $con);
	
			/* The following can be used instead of the line above to
			   guarantee the related object contains a reference
			   to this object, but this level of coupling
			   may be undesirable in many circumstances.
			   As it can lead to a db query with many results that may
			   never be used.
			   $obj = MLUseLanguagePeer::retrieveByPK($this->languageid, $con);
			   $obj->addMLUseTexts($this);
			 */
		}
		return $this->aMLUseLanguage;
	}

	/**
	 * Provides convenient way to set a relationship based on a
	 * key.  e.g.
	 * <code>$bar->setFooKey($foo->getPrimaryKey())</code>
	 *
 
	 * @return void
	 * @throws PropelException
	 */
	public function setMLUseLanguageKey($key)
	{

		$this->setLanguageid( (int) $key);			
		
	}



 
	/**
	 * Removes this object from datastore and sets delete attribute.
	 * 
	 * @param Connection $con
	 * @return void
	 * @throws PropelException
	 * @see BaseObject::setDeleted()
	 * @see BaseObject::isDeleted()
	 */
	public function delete($con = null)
	{	
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}
		
		if ($con === null)
			$con = Propel::getConnection(MLUseTextPeer::DATABASE_NAME);

		try {
			$con->begin();
			MLUseTextPeer::doDelete($this, $con);
			$this->setDeleted(true);
			$con->commit();
		} catch (PropelException $e) {
			$con->rollback();
			throw $e;
		}
	}
	

	/**
	 * flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var boolean
	 */
	private $alreadyInSave = false;

	/**
	 * Stores the object in the database.  If the object is new,
	 * it inserts it; otherwise an update is performed.  This method
	 * wraps the doSave() worker method in a transaction.
	 *
	 * @param Connection $con
	 * @return void
	 * @throws PropelException
	 */
	public function save($con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}
		
		if ($con === null)
			$con = Propel::getConnection(MLUseTextPeer::DATABASE_NAME);

		try {
			$con->begin();
			$this->doSave($con);			
			$con->commit();
		} catch (PropelException $e) {
			$con->rollback();
			throw $e;
		}
	}
  
	/**
	 * Stores the object in the database.  If the object is new,
	 * it inserts it; otherwise an update is performed.
	 *
	 * @param Connection $con
	 * @return void
	 * @throws PropelException
	 */
		protected function doSave($con)	{ 
		
	 
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.
 
			if ($this->aMLUseLanguage !== null) {
				if ($this->aMLUseLanguage->isModified()) $this->aMLUseLanguage->save($con);
				$this->setMLUseLanguage($this->aMLUseLanguage);
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = MLUseTextPeer::doInsert($this, $con);
					$this->setId( $pk );  //[IMV] update autoincrement primary key
					$this->setNew(false);
				} else {
					MLUseTextPeer::doUpdate($this, $con);
				}			
				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			$this->alreadyInSave = false;
		}
	}

	/**
	 * Validates the objects modified field values.
	 * This includes all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param mixed $columns Column name or an array of column names.
	 *
	 * @return mixed <code>true</code> if all columns pass validation
	 *			  or an array of <code>ValidationFailed</code> objects for columns that fail.
	 */
	public function validate($columns = null)
	{
	  if ($columns)
	  {
		return MLUseTextPeer::doValidate($this, $columns);
	  }

		return $this->doValidate();
	}

	/**
	 * flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var boolean
	 */
	protected $alreadyInValidation = false;
  
	/**
	 * This function performs the validation work for complex object models.
	 * 
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @return mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate()
	{
		if (! $this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;
	  
			$failureMap = array();	  
	  
			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.
			if ($this->aMLUseLanguage !== null) {
				if (($retval = $this->aMLUseLanguage->validate()) !== true) {
					$failureMap = array_merge($failureMap, $retval);
				}
			}

			if (($retval = MLUseTextPeer::doValidate($this)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}
	  
	  
			$this->alreadyInValidation = false;
		}
	
		return (!empty($failureMap) ? $failureMap : true);
	}
  


	private $pks = array();
	
	/**
	 * Set the PrimaryKey.
	 *
	 * @param array $keys The elements of the composite key (order must match the order in XML file).
	 * @return void
	 * @throws PropelException
	 */
	public function setPrimaryKey($keys)
	{						

		$this->setId($keys[0]);

		$this->setLanguageid($keys[1]);

	}


	/**
	 * Returns an id that differentiates this object from others
	 * of its class.
	 * @return array 
	 */
	public function getPrimaryKey()
	{
		$this->pks[0] = $this->getId();
		$this->pks[1] = $this->getLanguageid();
		return $this->pks;

	}

	
	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *  
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 *  
	 * @return MLUseText Clone of current object.
	 * @throws PropelException
	 */
	public function copy($deepCopy = false) 
	{
		$copyObj = new MLUseText();
		$copyObj->setText($this->text);
		
		$copyObj->setNew(true);
		
		$copyObj->setId(NULL); // this is a pkey column, so set to default value
		$copyObj->setLanguageid(NULL); // this is a pkey column, so set to default value

		return $copyObj;
	}
		

	/**
	 * returns a peer instance associated with this om.  Since Peer classes	
	 * are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 * @return MLUseTextPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new MLUseTextPeer();
		}
		return self::$peer;
	}

}
